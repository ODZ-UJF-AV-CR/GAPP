export enum Plugins {
    INFLUXDB = 'influxdb',
    POSTGRESDB = 'postgresdb',
    SONDEHUB = 'sondehub',
    SERVICES = 'services',
    REPOSITORIES = 'repositories',
    EVENT_BUS = 'eventBus',
    ABORT_CONTROLLER = 'abortController',
}
