export enum Plugins {
    INFLUXDB = 'influxdb',
    MONGODB = 'mongodb',
    SONDEHUB = 'sondehub',
    SERVICES = 'services',
    EVENT_BUS = 'eventBus',
    ABORT_CONTROLLER = 'abortController',
}
